export enum Gender {
  Nam,
  Nữ,
  Khác,
}

export enum Roles {
  USER = "USER",
  ADMIN = "ADMIN",
}

export enum CreateRoles {
  USER,
  ADMIN,
}

export enum CommonStatus {
  ACTIVE,
  INACTIVE,
}

export enum CommonStatusString {
  ACTIVE = "ACTIVE",
  INACTIVE = "INACTIVE",
}

export enum TripStatus {
  PENDING = "PENDING",
  OPENING = "OPENING",
  DEPARTED = "DEPARTED",
  COMPLETED = "COMPLETED",
  CANCELED = "CANCELED"
}
